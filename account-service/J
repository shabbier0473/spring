pipeline{
    agent { label "maven" }
    tools { maven "MAVEN_HOME" }
    stages{
        stage("compile"){
            dir ('account_service'){
                sh "mvn compile"
            }
        }
    }
}
