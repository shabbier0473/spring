pipeline{
    agent { label "maven" }
    tools { maven "MAVEN_HOME" }
    stages{
        stage("compile"){
            steps{
                sh "cd account-service && mvn compile  && mvn install"
            }
        }
        stage{
            steps{
                build job : "customer-service"
            }
        }
    }
}
